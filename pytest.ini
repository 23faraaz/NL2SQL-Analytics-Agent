[pytest]
markers =
    integration: requires a real, reachable PostgreSQL server. Skipped
        (not failed) when one is unavailable -- run `pytest -m integration`
        specifically to confirm these actually ran rather than skipped.
testpaths = tests
